// import { ProductOptions, ProductOptionValues } from "@/payload-types"

// export type ArrayElement<ArrayType extends readonly unknown[]> =
//   ArrayType extends readonly (infer ElementType)[] ? ElementType : never

// export type ProductOption = ArrayElement<NonNullable<ProductOptions>>
// export type ProductOptionValue = ArrayElement<NonNullable<ProductOptionValues>>
